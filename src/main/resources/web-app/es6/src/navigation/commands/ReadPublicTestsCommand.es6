'use strict';

class ReadPublicTestsCommand extends AbstractReadPublicTestsCommand {
    execute() {
        return new Promise((resolve) => {
			resolve();
        });
    }
}

/*       S.D.G.       */
