'use strict';

class ReadPrivateLessonsCommand extends AbstractReadPrivateLessonsCommand {
    execute() {
        return new Promise((resolve) => {
			resolve();
        });
    }
}

/*       S.D.G.       */
