'use strict';

class ReadPublicTestCommand extends AbstractReadPublicTestCommand {
    execute() {
        return new Promise((resolve) => {
			resolve();
        });
    }
}

/*       S.D.G.       */
