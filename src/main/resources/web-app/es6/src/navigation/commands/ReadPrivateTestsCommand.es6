'use strict';

class ReadPrivateTestsCommand extends AbstractReadPrivateTestsCommand {
    execute() {
        return new Promise((resolve) => {
			resolve();
        });
    }
}

/*       S.D.G.       */
