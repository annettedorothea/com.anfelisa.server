'use strict';

class IsTestFinishedCommand extends AbstractIsTestFinishedCommand {
    execute() {
        return new Promise((resolve) => {
			resolve();
        });
    }
}

/*       S.D.G.       */
