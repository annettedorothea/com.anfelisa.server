'use strict';

class LoadBoxCreationDataCommand extends AbstractLoadBoxCreationDataCommand {
    execute() {
        return new Promise((resolve) => {
			resolve();
        });
    }
}

/*       S.D.G.       */
